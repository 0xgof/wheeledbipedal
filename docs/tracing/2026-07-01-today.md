# 2026-07-01 Work Trace

Today's job is to finish orienting the repository around the controller-candidate
registry, then begin the foundational reduced-DOF simulation path.

Order:

1. Confirm the current MLOps-style registry is usable as the experiment memory.
2. Keep `docs/tickets/index.md` aligned with completed and next tickets.
3. Start `MECH-1`: document true DOF, constraints, actuator inputs, reduced
   coordinates, and observable vs privileged state.
4. Use `MECH-1` to drive `FL-1`: the reduced-order mechanism model.

Boundary:

- Do not jump to Isaac, MuJoCo, Brax/MJX, or Gazebo implementation yet.
- Do not scaffold empty architecture ahead of need.
- Keep the next code work focused on the Layer 1 DOF solver foundation.
